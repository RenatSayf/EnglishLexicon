<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto"
      xmlns:tools="http://schemas.android.com/tools"
      tools:context=".b_AddWordActivity">

    <item android:id="@+id/menu_tests"
          android:title="@string/tests"
          android:orderInCategory="100"
          app:showAsAction="never"/>

    <item android:id="@+id/menu_play_list"
          android:title="@string/play_list"
          android:orderInCategory="100"
          app:showAsAction="never"/>

    <item android:id="@+id/menu_word_editor"
          android:title="@string/edit_word"
          android:orderInCategory="100"
          app:showAsAction="never"/>

</menu>
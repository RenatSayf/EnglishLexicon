<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/color_status_bar</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="statusBarBackground">@color/color_status_bar</item>
    </style>
    <style name="AppTheme.NoActionBar" parent="AppTheme">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.Launcher">
        <item name="android:windowBackground">@drawable/side_nav_bar</item>
        <item name="android:windowSplashScreenAnimatedIcon" tools:targetApi="s">@android:color/transparent</item>
    </style>

    <style name="AppToolbarStyle" parent="ThemeOverlay.AppCompat.Light">
        <item name="android:textColorSecondary">@color/colorWhite</item>
        <item name="android:editTextColor">@color/colorWhite</item>
        <item name="colorAccent">@color/colorWhite</item>
    </style>

    <style name="spinner_style">
        <item name="android:background">@drawable/gradient_spinner</item>
        <item name="android:paddingLeft">10dp</item>
    </style>

    <!--Hint overridden theme for popapp-->
    <style name="popAppWindow" parent="@style/Theme.AppCompat.Dialog.Alert">
        <item name="android:colorBackground">@color/colorTransparent</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:textColor">@color/colorTransparent</item>
    </style>

    <!--Hint overridden them for transparent activity-->

    <style name="AppBottomDialog" parent="Theme.MaterialComponents.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/BottomSheetStyleWrapper</item>
    </style>

    <style name="BottomSheetStyleWrapper" parent="Widget.Design.BottomSheet.Modal">
        <item name="android:background">@android:color/transparent</item>
    </style>

    <style name="popupWindowAnimation">
        <item name="android:windowEnterAnimation">@anim/popup_anim</item>
        <item name="android:windowExitAnimation">@anim/popdown_anim</item>
    </style>

    <style name="scaleDownAndYtoUpAnimation">
        <item name="android:windowEnterAnimation">@anim/popup_anim</item>
        <item name="android:windowExitAnimation">@anim/anim_scale_down_y_to_up</item>
    </style>

</resources>
